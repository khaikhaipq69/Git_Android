<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Session08" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Session08" parent="Base.Theme.Session08" />
    <style name="btn">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#DF0909</item>
        <item name="android:textSize">50dp</item>
    </style>
    <style name="MyTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textSize">40dp</item>
        <item name="android:textColor">#009698</item>
    </style>
</resources>